{% extends '::base.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_rest(form) }}
    <br/>
    <input type="submit" VALUE="Save" class="btn btn-large btn-primary" id="submit">
    {{ form_end(form) }}
{% endblock %}